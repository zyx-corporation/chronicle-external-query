# v0.2.0 Completion Checklist

This checklist records the completion evidence for the first supported local
downstream runtime baseline of `chronicle-external-query`.

## Scope Decision

- [x] Milestone A completed for the first supported baseline
- [x] Milestone B completed for the first supported baseline
- [x] Milestone C completed for the first supported baseline
- [x] Milestone D completed for the first supported baseline
- [x] no remaining blocking gap was identified for the local-only supported path

## Validation Evidence

- [x] representative sanitized Chronicle bundle fixture is committed in
  `tests/fixtures/query_engine_bundle/representative_cli_bundle/`
- [x] bundle ingest validates required files, required keys, contract versions,
  and cross-file consistency
- [x] machine-readable ingest failure categories are available in CLI JSON
- [x] retrieval regression covers graph-only, hybrid overlap, vector-only, and
  representative bundle scenarios
- [x] runtime regression covers representative queries and repeated-run
  comparability
- [x] markdown trial and comparison reports render from saved artifacts

## Reproducibility Evidence

- [x] `.venv/bin/pytest -q` passes in the working checkout
- [x] `bash scripts/smoke_clean_checkout.sh` passes in the working checkout
- [x] `bash scripts/smoke_clean_checkout.sh` passes from a fresh copy without
  `.git`, `.venv`, or `.pytest_cache`
- [x] CI is wired to the same clean-checkout smoke path

## Boundary Evidence

- [x] no Chronicle write-back occurs in the supported local-only flow
- [x] no hosted vector provider is required for the supported local-only flow
- [x] runtime, retrieval, and artifact outputs remain downstream-derived and
  non-authoritative over Chronicle primary records
- [x] representative hybrid validation uses only checked-in local fixtures

## Release Decision

- [x] the repository can be handed off as the first supported local downstream
  runtime baseline
- [x] the repository is suitable for release as `v0.2.0`
