# v0.3.1

Release date: 2026-07-01

## Supported Boundary

- deterministic local smoke remains the primary release gate
- same-checkout operator preflight is now available as a faster day-to-day confirmation path
- Chronicle primary records remain authoritative and unchanged

## What Changed Since v0.3.0

- added `bash scripts/operator_preflight.sh` for current-checkout verification
- documented the preflight path in the main README and operator runbook
- added script-level regression coverage for the preflight helper

## Release Evidence

- `bash scripts/operator_preflight.sh --help` documents the supported inputs
- dry-run preflight covers editable install, `pytest`, representative bundle validation, representative hybrid query, and markdown trial report rendering
- `.venv/bin/pytest -q` passes for the current working tree

## Final Authority Boundary

- Chronicle write-back remains out of scope for this repository
- downstream artifacts remain downstream review support only
- operator preflight is a local convenience check, not a hosted deployment or correctness proof
