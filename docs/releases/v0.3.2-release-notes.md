# v0.3.2

Release date: 2026-07-01

## Supported Boundary

- deterministic local smoke remains the primary release gate
- same-checkout operator evidence capture is now available after local verification
- Chronicle primary records remain authoritative and unchanged

## What Changed Since v0.3.1

- added `bash scripts/operator_capture.sh` to collect a reviewable local evidence bundle
- documented the capture path in the main README and operator runbook
- added script-level regression coverage for the capture helper and plugin env bootstrap helper

## Release Evidence

- `bash scripts/operator_capture.sh --help` documents the supported inputs
- dry-run capture covers bundle validation, bundle summary export, hybrid query artifact export, and markdown trial report rendering
- `.venv/bin/pytest -q` passes for the current working tree

## Final Authority Boundary

- Chronicle write-back remains out of scope for this repository
- downstream artifacts remain downstream review support only
- operator capture preserves local review evidence, not a hosted deployment or correctness proof
